module.exports = class Requester {
  constructor() {}

  /**
   * @param {string} body String the path the requester is sending it to.
   * @param {boolean} auth Sometimes auth is needed. 
   */
  start(body, auth) {}
};
